# Pi Context VS Code extension

Scaffold for Architecture 1. The `Pi Context: Attach Editor Selection` command captures a selection through an Effect program but deliberately does not send it yet.

Run `npm install` at the repository root, then open this directory in VS Code and launch the extension host with `F5` after implementation adds a launch configuration.

